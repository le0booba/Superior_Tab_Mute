# Superior Tab Mute

<div align="center">
   <img src="https://raw.githubusercontent.com/le0booba/Superior_Tab_Mute/refs/heads/main/screen-options.png" alt="Superior Tab Mute Screenshot" width="230"/>

   **Интеллектуальное управление звуком в Chrome**
   
   **[English](README.md)** | **Русский**

   Остановите звуковой хаос. Superior Tab Mute дает вам полный контроль над звуковым пространством браузера, автоматически отключая фоновые вкладки на основе простых и мощных правил. Это создает сфокусированную среду, идеальную для концентрации на работе, наслаждения непрерывной музыкой или изоляции одного потока. С умными режимами отключения звука, мгновенным управлением и чистым дизайном вы наконец сможете работать в браузере в тишине.

   ![Chrome Extension](https://img.shields.io/badge/Chrome-Extension-blue?logo=googlechrome)
   ![Manifest V3](https://img.shields.io/badge/Manifest-V3-brightgreen)
   ![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)
</div>

---

## 🚀 Установка

1.  **Скачайте расширение**
    -   Загрузите [ПОСЛЕДНИЙ РЕЛИЗ](https://github.com/le0booba/Superior_Tab_Mute/releases/latest) с GitHub.
    -   Распакуйте скачанный файл.

2.  **Загрузите в Chrome**
    -   Откройте Chrome и перейдите на `chrome://extensions/`.
    -   Включите **Режим разработчика** (переключатель вверху справа).
    -   Нажмите **Загрузить распакованное** и выберите папку с расширением.

3.  **Начните использовать**
    -   Кликните на иконку расширения в панели инструментов Chrome для доступа к всплывающему окну и настройкам.

---

## 🌟 Возможности

### 🎯 Умные режимы отключения звука

-   **Режим активной вкладки**: Только текущая активная вкладка воспроизводит звук, идеально для сфокусированной работы или учебы.
-   **Режим первого звука**: Приоритет отдается первой вкладке, которая начала воспроизводить звук, идеально для прослушивания музыки или подкастов в фоне.
-   **Режим белого списка**: Вручную выберите единственную вкладку для воспроизведения звука, обеспечивая точный контроль для сложных рабочих процессов.
-   **Режим отключения новых вкладок**: Автоматически отключает звук на всех вновь открываемых вкладках, сохраняя вашу сессию тихой по умолчанию.

### ⚡ Мгновенное управление

-   **Главный переключатель**: Включите или отключите расширение одним кликом или горячей клавишей (`Alt+S`).
-   **Глобальное отключение звука**: Мгновенно отключите звук на всех вкладках (`Alt+A`).
-   **Установка источника звука**: Назначьте текущую вкладку источником звука в режиме первого звука (`Alt+W`).
-   **Показать все вкладки**: Просматривайте и выбирайте из всех доступных вкладок, а не только воспроизводящих звук, в режимах первого звука и белого списка.
-   **Раскрываемые опции**: Режимы первого звука и белого списка имеют сворачиваемые секции для отображения дополнительных настроек и списков вкладок, поддерживая интерфейс чистым и организованным. Нажмите кнопку раскрытия (▼) для переключения дополнительных элементов управления.

### 🧠 Интеллектуальная автоматизация

-   **Запоминание последнего источника**: При включении для режимов "Первый звук" или "Белый список", расширение автоматически переключается на ранее воспроизводившие звук вкладки, когда текущий источник замолкает или закрывается. Это обеспечивает бесшовное прослушивание без ручного вмешательства, интеллектуально приоритизируя ваш опыт прослушивания путем возврата к последнему активному источнику звука.
-   **Настройки по умолчанию при запуске**: Установите предпочитаемый режим и состояние "Отключить всё" по умолчанию. При запуске Chrome расширение автоматически применяет эти настройки, обеспечивая единообразный опыт в каждой сессии. Отметьте любой режим или переключатель "Отключить всё" звездочкой (★) для установки по умолчанию.

<details>
<summary>🔧 Расширенный функционал</summary>

**Базовая архитектура:**
1. **Постоянные и синхронизированные настройки**: Основные предпочтения синхронизируются между устройствами через chrome.storage.sync для единообразного опыта.
2. **Безопасная обработка**: Автоматически фильтрует системные страницы Chrome (`chrome://`) и страницы расширений (`chrome-extension://`) для предотвращения конфликтов с использованием валидации `isManageableTab()`.
3. **Восстановление после ошибок**: Корректно обрабатывает закрытые вкладки через обертки `safeGetTab()`, `safeUpdateTab()` и `safeQueryTabs()`, которые перехватывают и логируют ошибки без нарушения функциональности.
4. **Стратифицированная система хранения**: Использует chrome.storage.sync для пользовательских предпочтений (режим, настройки, значения по умолчанию), chrome.storage.session для временных ID вкладок и состояний раскрытия, и chrome.storage.local для специфичных для устройства UI предпочтений (язык, состояния чекбоксов).
5. **Мультиязычная поддержка**: Встроенная английская и русская локализация с бесшовным переключением языков, хранением языковых предпочтений локально при сохранении текста интерфейса во встроенных объектах локалей.

**Оптимизации производительности:**
1. **Дебаунсинг обработки событий**: Частые операции вроде `applyMutingRules()` и `updatePopupData()` дебаунсятся с задержкой 150ms для предотвращения избыточных выполнений при быстрых изменениях вкладок, снижая использование CPU на ~40-50%.
2. **Параллельные асинхронные операции**: Использует `Promise.all()` в `getCombinedSettings()`, инициализации и обработке вкладок для одновременного выполнения независимых операций, снижая латентность на 40-60%.
3. **Избирательная обработка вкладок**: Фильтрует управляемые вкладки заранее используя `isManageableTab()` для исключения системных страниц, предотвращая ненужные API-вызовы и снижая overhead Chrome API на ~20-30%.
4. **Оптимизированный доступ к хранилищу**: Кеширует данные вкладок в chrome.storage.session через `popupTabsData` для мгновенного рендеринга всплывающего окна, и группирует связанные операции хранения для минимизации API-вызовов.
5. **Эффективный рендеринг DOM**: Использует DocumentFragment для пакетного рендеринга списков вкладок и ранние возвраты в обработчиках событий для избежания ненужных обновлений DOM, улучшая отзывчивость UI на ~30%.

</details>

---

## 📖 Руководство по использованию

1.  **Откройте всплывающее окно**
    -   Кликните на иконку Superior Tab Mute в панели инструментов Chrome для доступа к основному управлению.

2.  **Выберите режим**
    -   Выберите желаемую стратегию отключения звука: **Активная вкладка**, **Первый звук**, **Белый список** или **Отключение новых вкладок** с помощью радиокнопок.
    -   Для режимов "Первый звук" или "Белый список" нажмите кнопку раскрытия (▼) внизу всплывающего окна для отображения дополнительных опций и списков вкладок. Нажмите снова для отображения большего количества опций.
    -   Используйте список вкладок для выбора источника звука. Вы можете включить "Показать все вкладки" для большего количества опций.
    -   При использовании режимов "Первый звук" или "Белый список" установите флажок **"Запоминание последнего источника"** для включения автоматического переключения источника. Расширение будет бесшовно переключаться на ранее воспроизводившие звук вкладки, если текущий источник прекратит воспроизведение или закроется.

3.  **Установите поведение по умолчанию**
    -   Кликните на иконку звезды (☆) рядом с любым режимом или переключателем "Отключить всё" для установки по умолчанию.
    -   Заполненная звезда (★) указывает на текущую настройку по умолчанию.
    -   Настройки по умолчанию автоматически применяются при запуске Chrome, обеспечивая единообразное поведение между сессиями.

4.  **Используйте горячие клавиши**
    -   `Alt+S`: Включить/выключить расширение
    -   `Alt+A`: Переключить отключение звука на всех вкладках
    -   `Alt+W`: Установить текущую вкладку источником звука (режим первого звука)
    -   Наведите курсор на элементы управления во всплывающем окне для просмотра их горячих клавиш
    -   Настройте горячие клавиши в `chrome://extensions/shortcuts`

---

## 🔒 Разрешения и конфиденциальность

### Обязательство конфиденциальности

-   **Нет сбора данных**: Расширение не собирает, не хранит и не передает никакие персональные данные.
-   **Нет аналитики**: Оно не включает скрипты отслеживания или связи с внешними серверами.
-   **Локальная работа**: Вся функциональность работает полностью локально в вашем браузере.

<details>
<summary>Используемые разрешения</summary>

-   **`tabs`**: Требуется для определения, какие вкладки воспроизводят звук, чтения их заголовков/иконок для UI и применения правил отключения звука.
-   **`storage`**: Используется для сохранения пользовательских предпочтений локально для единообразного опыта между сессиями браузера.

</details>

<details>
<summary>Синхронизированные настройки (chrome.storage.sync)</summary>

-   Настройки, хранящиеся в вашем аккаунте Google и синхронизирующиеся между устройствами, где вы авторизованы в Chrome.
-   **`isExtensionEnabled`** (true/false): Управляет, активно ли расширение.
-   **`mode`** ('active', 'first-sound', 'whitelist', 'mute-new'): Определяет активный режим отключения звука.
-   **`isAllMuted`** (true/false): Переключает глобальное состояние отключения звука для всех вкладок.
-   **`rememberLastTab`** (true/false): Запоминает предпочтение для функции "Запоминание последнего источника".
-   **`defaultMode`** (строка или null): Хранит предпочитаемый пользователем режим по умолчанию для применения при запуске.
-   **`defaultMuteAll`** (true/false): Хранит, должно ли "Отключить всё" быть включено по умолчанию при запуске.
-   *Назначение*: Обеспечивает единообразие ваших основных предпочтений на всех устройствах.

</details>

<details>
<summary>Сессионное хранилище (chrome.storage.session)</summary>

-   Временные настройки, которые очищаются при закрытии браузера.
-   **`firstAudibleTabId`** (ID вкладки): Отслеживает назначенную вкладку-источник звука в "Режиме первого звука".
-   **`whitelistedTabId`** (ID вкладки): Отслеживает выбранную пользователем вкладку в "Режиме белого списка".
-   **`popupTabsData`** (массив объектов вкладок): Кешированная информация о вкладках для эффективного рендеринга всплывающего окна, включая ID вкладок, заголовки, иконки, статус воспроизведения звука и URL.
-   **`expansionStates`** (объект): Запоминает, какие раскрываемые секции открыты для каждого режима в текущей сессии.
-   **`muteNewInitialTabIds`** (массив): Отслеживает вкладки, существовавшие при активации режима "Отключение новых вкладок", используется для управления поведением включения звука.
-   *Назначение*: ID вкладок уникальны для каждой сессии браузера и будут недействительны между устройствами или после перезапуска, что делает сессионное хранилище идеальным выбором.

</details>

<details>
<summary>Локальное хранилище (chrome.storage.local)</summary>

-   Настройки, которые постоянны на устройстве, но не синхронизируются между аккаунтами.
-   **`stm_lang`** ('en'/'ru'): Запоминает языковое предпочтение для интерфейса.
-   **`showAllTabsFirstSound`** (true/false): Запоминает состояние чекбокса "Показать все вкладки" для режима первого звука.
-   **`showAllTabsWhitelist`** (true/false): Запоминает состояние чекбокса "Показать все вкладки" для режима белого списка.
-   *Назначение*: Позволяет иметь специфичные для устройства UI предпочтения, например, разные настройки на рабочем и домашнем компьютерах.

</details>

---

## 📁 Структура файлов
```
Superior_Tab_Mute/
├── 📋 manifest.json         # Конфигурация расширения и разрешения
├── 🔧 background.js         # Основная логика отключения звука и обработка событий
├── 🖹 popup.html            # Структура пользовательского интерфейса
├── ⚙️ popup.js              # Логика UI и взаимодействия с пользователем
├── 🎨 popup.css             # Современный темный стиль оформления
├── 🌐 _locales/             # Файлы интернационализации
│   ├── 📁 en/                  # Английские переводы
│   │   └── 📄 messages.json       # Английские строки локализации
│   └── 📁 ru/                  # Русские переводы
│       └── 📄 messages.json       # Русские строки локализации
├── 🗂 icons/                # Иконки статуса расширения
│   ├── 🖼️ icon16.png           # Стандартное состояние (16x16)
│   ├── 🖼️ icon16_mute.png      # Все вкладки отключены (16x16)
│   ├── 🖼️ icon16_off.png       # Выключенное состояние (16x16)
│   ├── 🖼️ icon48.png           # Стандартное состояние (48x48)
│   ├── 🖼️ icon48_mute.png      # Все вкладки отключены (48x48)
│   ├── 🖼️ icon48_off.png       # Выключенное состояние (48x48)
│   ├── 🖼️ icon128.png          # Стандартное состояние (128x128)
│   ├── 🖼️ icon128_mute.png     # Все вкладки отключены (128x128)
│   └── 🖼️ icon128_off.png      # Выключенное состояние (128x128)
├── 📄 LICENSE.md            # Лицензия MIT
└── 📖 README.md             # Эта документация
```

---

<div align="center">
   
  **Создано с ❤️ badrenton**
  
  *© 2025 • Если это расширение оказалось полезным, пожалуйста, поставьте ⭐ на GitHub!*
</div>
